on:
    push:
        branches:
            ['master']









jobs:
    build:
        runs-on: ubuntu-latest
        

        steps:
             - uses: actions/checkout@v3
             - name: "login to docker "
               run: docker login --username momenrama --password dckr_pat_uqE8RvdMum5HMI7HwPV0bhEgpmQ